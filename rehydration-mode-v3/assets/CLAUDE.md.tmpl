# Project: {项目名称}

<!--
  这是 Claude Code 每次进入项目目录时自动加载的指令文件。
  请根据实际项目情况替换所有 {占位符}。
-->

## 强制规则

- **每次会话开始时，必须先读取 `docs/PROJECT_MEMORY.md`（热记忆层）和 `docs/CURRENT_TASK.md`**
- **每次会话结束时，必须更新 `docs/SESSION_LOG.md`（追加写入）**
- **任何架构决策必须同步到 `docs/DECISIONS.md`**
- **不要修改 `docs/` 目录下的文件结构**
- **不要在模型间切换同一任务（保持任务连续性）**

## 技术栈

- 后端: {后端语言 + 框架 + ORM + 数据库}
- 前端: {前端框架 + 语言 + UI 库}
- 中间件: {消息队列/缓存/注册中心/搜索等}
- 部署: {部署方式}

## 代码规范

- {规范条目 1}
- {规范条目 2}
- {规范条目 3}

## 禁止事项

- 不要直接操作生产数据库
- 不要提交包含密钥的代码
- {其他禁止事项}

## 记忆文件位置

- 项目整体记忆: `docs/PROJECT_MEMORY.md`（热/温/冷三层结构）
- 当前任务: `docs/CURRENT_TASK.md`
- 会话日志: `docs/SESSION_LOG.md`（当前周）+ `docs/session-log-YYYY-WXX.md`（历史周）
- 架构决策: `docs/DECISIONS.md`

## 会话启动协议

每次新会话启动时，必须按以下顺序执行：

1. **读取** `docs/CURRENT_TASK.md`（必读）—— 了解当前阶段、进行中任务、阻塞项
2. **读取** `docs/PROJECT_MEMORY.md` 热记忆层（必读）—— 了解项目核心信息
3. **检查** `docs/SESSION_LOG.md` 是否需要跨周封存（调用 `session_log_manager.py archive`）
4. **汇报** 当前工作状态给用户（使用结构化格式：阶段 + 进行中任务 + 阻塞项 + 建议）
5. **询问** 用户是否继续当前任务

## 会话结束协议

每次会话结束前，必须按以下顺序执行：

1. **更新** `docs/CURRENT_TASK.md` —— 同步任务进度、记录当前编辑位置、更新阻塞项
2. **追加** `docs/SESSION_LOG.md` —— 记录本次会话时间、完成的任务、关键决策、遗留工作（小时级条目）
3. **检查** 是否需要日汇总条目（调用 `session_log_manager.py ensure_daily`）
4. **同步** `docs/DECISIONS.md` —— 如有新架构决策，追加到决策记录
5. **提示** 用户："记忆已持久化，下次启动时会自动恢复当前状态"

## claude-mem 集成

如果安装了 claude-mem 插件，本 Skill 与其分工如下：

| 职责 | rehydration-mode-v3 | claude-mem |
|------|---------------------|------------|
| 项目架构记忆 | ✅ PROJECT_MEMORY.md（结构化） | ❌ 不重复 |
| 当前任务状态 | ✅ CURRENT_TASK.md | ❌ 不重复 |
| 会话进度日志 | ✅ SESSION_LOG.md（小时+天+周） | ❌ 不重复 |
| 架构决策记录 | ✅ DECISIONS.md（ADR） | ❌ 不重复 |
| 会话原始内容 | ❌ 不存储 | ✅ claude-mem 存储 |
| 代码片段/命令历史 | ❌ 不存储 | ✅ claude-mem 存储 |
| 自动触发恢复 | ✅ 会话开始时 | ✅ 按需查询 |

**去重原则**：rehydration-mode-v3 存储「结构化摘要」，claude-mem 存储「原始会话内容」。恢复上下文时，先读本 Skill 的热记忆层获取全局状态，再按需查询 claude-mem 获取具体代码/命令细节。两者不冗余。
