# 会话日志 — {WEEK_ID}

<!--
  ⚠️ V3 改进：小时级条目 + 天级汇总 + 周文件封存

  文件命名规则：
  - docs/SESSION_LOG.md              — 当前周的活跃日志
  - docs/session-log-YYYY-WXX.md     — 历史周封存文件（如 session-log-2026-W27.md）

  写入规则：
  - 小时级条目：每次会话结束追加到文件末尾（最新在最下面）
  - 天级汇总：每天第一次会话时创建 ## YYYY-MM-DD 日汇总 标题，当天最后一次会话后填写汇总内容
  - 周封存：跨周时自动将当前文件内容归档到 session-log-YYYY-WXX.md，重置当前文件

  读取规则：
  - 默认读取 docs/SESSION_LOG.md 的最新条目
  - 如需历史记录，读取对应的 session-log-YYYY-WXX.md

  调用 session_log_manager.py 管理封存的命令：
  python <skill_dir>/scripts/session_log_manager.py <project_root> archive
  python <skill_dir>/scripts/session_log_manager.py <project_root> read_latest
-->

**周期**: {YYYY-MM-DD} ~ {YYYY-MM-DD}（第 {XX} 周）

---

## {YYYY-MM-DD} 日汇总

{当天所有会话的关键信息汇总，在当天最后一次会话后填写}

---

### {HH:MM} {会话标题}

**任务**: {用 1-2 句话描述本次会话的目标}

**完成的工作**:

1. {完成的工作项 1}
   - 文件: `{file_path}`（{行数}行）
   - 包含: {功能点描述}

2. {完成的工作项 2}
   - 文件: `{file_path}`
   - 包含: {功能点描述}

**关键决策**:
- 决定: {决策内容}
- 原因: {决策原因}
- 记录: 见 `docs/DECISIONS.md#adr-xxx`

**遇到的问题**:

1. {问题描述}
   - 解决: {解决方案（或"暂未解决，记录为阻塞项"）}

**遗留工作**:
- {遗留工作项 1}
- {遗留工作项 2}

**代码变更**:

```bash
git diff --stat
 {file_1}  | {+++} / {---}
 {file_2}  | {+++} / {---}
```

<!--
  ========================================================
  使用说明：
  1. 每次会话结束，复制上方的 ### HH:MM 模板追加到文件末尾
  2. 日汇总条目在当天第一次会话时创建（标题），最后一次会话时填写内容
  3. 不要删除历史条目，最新的永远在最下面
  4. 跨周时运行 archive 命令自动封存
  ========================================================
-->
