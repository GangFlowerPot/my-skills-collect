# Project: {项目名称}

<!--
  这是 Claude Code 每次进入项目目录时自动加载的指令文件。
  请根据实际项目情况替换所有 {占位符}。
  如果某条规则不适用，可以删除对应行。
-->

## 强制规则

- **每次会话开始时，必须先读取 `docs/PROJECT_MEMORY.md` 和 `docs/CURRENT_TASK.md`**
- **每次会话结束时，必须更新 `docs/SESSION_LOG.md`**
- **任何架构决策必须同步到 `docs/DECISIONS.md`**
- **不要修改 `docs/` 目录下的文件结构**
- **不要在模型间切换同一任务（保持任务连续性）**

## 技术栈

<!-- 根据实际项目填写，以下为示例 -->

- 后端: {后端语言 + 框架 + ORM + 数据库}
- 前端: {前端框架 + 语言 + UI 库}
- 部署: {部署方式}
- 其他: {消息队列、缓存、搜索引擎等}

## 代码规范

<!-- 根据团队规范填写 -->

- {规范条目 1}
- {规范条目 2}
- {规范条目 3}

## 禁止事项

<!-- 根据项目约束填写 -->

- 不要直接操作生产数据库
- 不要提交包含密钥的代码
- {其他禁止事项}

## 记忆文件位置

- 项目整体记忆: `docs/PROJECT_MEMORY.md`
- 当前任务: `docs/CURRENT_TASK.md`
- 会话日志: `docs/SESSION_LOG.md`
- 架构决策: `docs/DECISIONS.md`

## 会话启动协议

每次新会话启动时，必须按以下顺序执行：

1. **读取** `docs/CURRENT_TASK.md`（必读）—— 了解当前阶段、进行中任务、阻塞项
2. **读取** `docs/PROJECT_MEMORY.md`（必读）—— 了解项目全局信息
3. **汇报** 当前工作状态给用户（使用结构化格式：阶段 + 进行中任务 + 阻塞项 + 建议）
4. **询问** 用户是否继续当前任务

## 会话结束协议

每次会话结束前，必须按以下顺序执行：

1. **更新** `docs/CURRENT_TASK.md` —— 同步任务进度、记录当前编辑位置、更新阻塞项
2. **追加** `docs/SESSION_LOG.md` —— 记录本次会话时间、完成的任务、关键决策、遗留工作
3. **同步** `docs/DECISIONS.md` —— 如有新架构决策，追加到决策记录
4. **提示** 用户："记忆已持久化，下次启动时会自动恢复当前状态"
