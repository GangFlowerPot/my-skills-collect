# 会话日志

<!--
  记录每次会话的摘要，用于追溯历史工作和决策过程。
  每次会话结束时追加一条新条目到文件顶部（最新在前）。
  建议每月归档一次旧日志（保留最近 3 个月）。
-->

---

## {YYYY-MM-DD} {HH:MM}-{HH:MM} [{模型名称}] 会话ID: {session_id}

### 任务

{用 1-2 句话描述本次会话的目标和任务}

### 完成的工作

1. {完成的工作项 1}
   - 文件: `{file_path}`（{行数}行）
   - 包含: {功能点描述}

2. {完成的工作项 2}
   - 文件: `{file_path}`
   - 包含: {功能点描述}

3. {完成的工作项 3}
   - 文件: `{file_path}`
   - 新增: {新增内容描述}

### 关键决策

- 决定: {决策内容}
- 原因: {决策原因}
- 记录: 见 `docs/DECISIONS.md#{ADR-XXX-决策标题}`

### 遇到的问题

1. {问题描述}
   - 解决: {解决方案}

2. {问题描述}
   - 解决: {解决方案（或"暂未解决，记录为阻塞项"）}

### 遗留工作

- {遗留工作项 1}
- {遗留工作项 2}
- {遗留工作项 3}

### 代码变更

```bash
git diff --stat
 {file_1}  | {+++} / {---}
 {file_2}  | {+++} / {---}
 {file_3}  | {+++} / {---}
 {N} files changed, {X} insertions(+), {Y} deletions(-)
```

<!--
  如果项目使用 Git，运行 `git diff --stat` 获取变更统计。
  如果不在 Git 仓库中，列出本次修改的文件清单。
-->

---

<!--
  以下为历史会话记录占位。
  新条目追加在最上方（替换此注释上方的内容）。

  示例格式：
  ## 2026-06-30 09:00-10:30 [Claude 4 Sonnet] 会话ID: sess_abc123

  ### 任务
  实现订单取消和退款流程

  ### 完成的工作
  1. 完成了订单取消接口的完整实现
     - 文件: `services/order/cancel.go`（120行）
     - 包含: 权限校验、状态检查、库存释放、数据库事务
  ...
-->
