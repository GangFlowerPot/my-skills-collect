# 项目记忆 — {项目名称}

<!--
  这是项目的整体记忆文件，记录全局信息。
  新成员加入、长时间未工作后恢复、需要了解项目全局时读取此文件。
  请根据实际项目情况替换所有 {占位符} 和示例内容。
-->

## 项目概述

{用 2-3 句话描述项目是什么，解决什么问题，核心能力有哪些}

## 架构概览

```mermaid
graph TD
    A[{用户端}] --> B[{API Gateway}]
    B --> C[{服务模块 1}]
    B --> D[{服务模块 2}]
    B --> E[{服务模块 3}]
    C --> F[({数据库})]
    D --> G[({数据库})]
    E --> H[({数据库})]
```

<!--
  提示：使用 Mermaid 语法绘制项目的架构图。
  可以用 graph TD（从上到下）、graph LR（从左到右）等。
  更多语法参考：https://mermaid.js.org/
-->

---

## 模块划分

| 模块 | 路径 | 职责 | 状态 | 负责人 |
|------|------|------|------|--------|
| {模块 1} | `{/path/to/module1}` | {该模块的职责描述} | {✅/🔄/⏳} | {负责人} |
| {模块 2} | `{/path/to/module2}` | {该模块的职责描述} | {✅/🔄/⏳} | {负责人} |
| {模块 3} | `{/path/to/module3}` | {该模块的职责描述} | {✅/🔄/⏳} | {负责人} |

<!-- 状态图例：✅ 已完成 | 🔄 开发中 | ⏳ 待开始 -->

## 数据模型

### 核心实体关系

```
{EntityA} 1:N {EntityB}
{EntityB} 1:1 {EntityC}
{EntityB} N:M {EntityD}
```

### 关键表/集合结构

- `{table_name_1}`: {表用途说明}
- `{table_name_2}`: {表用途说明}
- `{table_name_3}`: {表用途说明}

## API 设计

### 核心接口

| 路径 | 方法 | 用途 | 状态 |
|------|------|------|------|
| `/api/v1/{resource}` | POST | {创建资源} | {✅/🔄/⏳} |
| `/api/v1/{resource}/:id` | GET | {查询资源详情} | {✅/🔄/⏳} |
| `/api/v1/{resource}/:id` | PUT | {更新资源} | {✅/🔄/⏳} |
| `/api/v1/{resource}/:id` | DELETE | {删除资源} | {✅/🔄/⏳} |

<!-- 状态图例：✅ 已完成 | 🔄 开发中 | ⏳ 待开始 -->

## 已完成的里程碑

- [x] {里程碑 1 — 例如 v0.1 项目脚手架搭建}
- [x] {里程碑 2}
- [x] {里程碑 3}
- [ ] {里程碑 4 — 当前进行中}
- [ ] {里程碑 5}
- [ ] {里程碑 6}

## 已知问题 / 技术债务

<!-- 按优先级排列：P0（阻塞）> P1（高）> P2（中）> P3（低） -->

1. **{问题标题}**
   - 问题: {具体问题描述}
   - 影响: {影响范围}
   - 计划: {解决计划，如"v0.6 优化"}
   - 优先级: {P0/P1/P2/P3}

2. **{问题标题}**
   - 问题: {具体问题描述}
   - 影响: {影响范围}
   - 计划: {解决计划}
   - 优先级: {P0/P1/P2/P3}

## 环境信息

- 开发环境: {开发环境地址}
- 测试环境: {测试环境地址}
- 数据库: {数据库连接信息（不含密码）}
- 缓存: {Redis/Memcached 等连接信息}
- 消息队列: {Kafka/RabbitMQ 等连接信息}
- 对象存储: {OSS/S3 等连接信息}
