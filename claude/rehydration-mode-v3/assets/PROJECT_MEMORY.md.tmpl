# 项目记忆 — {项目名称}

<!--
  ⚠️ V3 改进：三层记忆模型 + 记忆压缩

  本文件采用「热/温/冷」三层结构，实现记忆的分级管理和自动压缩：

  🔥 热记忆（Hot）   — 最近 7 天频繁使用的核心信息
     → 保持完整，不压缩
     → 包含：架构概览、当前模块、活跃 API、关键约定

  🌡️ 温记忆（Warm）  — 7-30 天内使用过的次要信息
     → 超过 14 天未引用的条目精简为 1-2 行摘要
     → 包含：已知问题、环境配置、模块细节、命名约定

  ❄️ 冷记忆（Cold）  — 超过 30 天或极少使用的历史信息
     → 保留标题 + 摘要指针（指向 SESSION_LOG 或 DECISIONS）
     → 包含：已废弃决策、旧版 API、历史环境、归档里程碑

  压缩规则：
  - 热记忆占比不超过总大小的 60%
  - 温记忆超过 100 行时触发压缩建议
  - 冷记忆超过 50 行时触发归档建议
  - 每月执行一次 memory_compressor.py analyze 检查结构健康度

  请根据实际项目情况替换所有 {占位符}。
-->

---

## 🔥 热记忆

### 项目概述

{用 2-3 句话描述项目是什么，解决什么问题，核心能力有哪些}

### 架构概览

```mermaid
graph TD
    A[{用户端}] --> B[{API Gateway}]
    B --> C[{服务模块 1}]
    B --> D[{服务模块 2}]
    B --> E[{服务模块 3}]
    C --> F[({数据库})]
    D --> G[({数据库})]
    E --> H[({数据库})]
```

### 当前活跃模块

| 模块 | 路径 | 职责 | 状态 |
|------|------|------|------|
| {模块 1} | `{/path/to/module1}` | {该模块的职责描述} | {✅/🔄/⏳} |
| {模块 2} | `{/path/to/module2}` | {该模块的职责描述} | {✅/🔄/⏳} |

### 核心 API（最近使用）

| 路径 | 方法 | 用途 | 最近使用 |
|------|------|------|----------|
| `/api/v1/{resource}` | POST | {创建资源} | {YYYY-MM-DD} |
| `/api/v1/{resource}/:id` | GET | {查询资源详情} | {YYYY-MM-DD} |

### 关键约定

- {命名约定 1}
- {代码组织约定}
- {分支/提交约定}

---

## 🌡️ 温记忆

### 数据模型

#### 核心实体关系

```
{EntityA} 1:N {EntityB}
{EntityB} 1:1 {EntityC}
{EntityB} N:M {EntityD}
```

#### 关键表/集合结构

- `{table_name_1}`: {表用途说明}
- `{table_name_2}`: {表用途说明}

### 已知问题 / 技术债务

<!-- 按优先级排列：P0（阻塞）> P1（高）> P2（中）> P3（低） -->

1. **{问题标题}**
   - 问题: {具体问题描述}
   - 影响: {影响范围}
   - 计划: {解决计划}
   - 优先级: {P0/P1/P2/P3}
   - 更新: {YYYY-MM-DD}

### 环境信息

- 开发环境: {开发环境地址}
- 测试环境: {测试环境地址}
- 数据库: {数据库连接信息（不含密码）}
- 缓存: {Redis/Memcached 等连接信息}
- 消息队列: {Kafka/RabbitMQ 等连接信息}

### 中间件配置

- 注册中心: {Nacos/Eureka/Consul}
- 配置中心: {Nacos Config/Apollo}
- API  Gateway: {Spring Cloud Gateway/Zuul}
- 负载均衡: {LoadBalancer/Ribbon}
- 熔断: {Hystrix/Sentinel}

---

## ❄️ 冷记忆

### 已废弃决策

<!-- 保留标题 + 指向 DECISIONS.md 的指针 -->

- **ADR-001: {决策标题}** — 已废弃，原因见 `docs/DECISIONS.md#adr-001`
- **ADR-002: {决策标题}** — 已废弃，原因见 `docs/DECISIONS.md#adr-002`

### 历史环境（归档）

- {旧环境说明} — 已于 {YYYY-MM-DD} 下线
- {旧环境说明} — 已于 {YYYY-MM-DD} 下线

### 历史 API（已废弃）

- `{旧 API 路径}` — 已于 {YYYY-MM-DD} 废弃，替代方案: {新 API}

### 已完成的里程碑

- [x] {里程碑 1 — 例如 v0.1 项目脚手架搭建} ({YYYY-MM-DD})
- [x] {里程碑 2} ({YYYY-MM-DD})
- [x] {里程碑 3} ({YYYY-MM-DD})

---

<!--
  📋 记忆维护提示：

  1. 热记忆：每次架构变更时更新，保持精简（< 200 行）
  2. 温记忆：每周 review 一次，超过 14 天未引用的条目精简为摘要
  3. 冷记忆：每月归档一次，详细内容移到 SESSION_LOG 或 DECISIONS
  4. 运行 `python <skill_dir>/scripts/memory_compressor.py <project_root> analyze` 检查结构健康度
  5. 运行 `python <skill_dir>/scripts/memory_compressor.py <project_root> compress` 获取压缩建议
-->
