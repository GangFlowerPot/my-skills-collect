# 记忆导航协议

`AGENT_MEMORY.md` 是唯一权威导航文件，必须包含 `schema`、`project`、`memory_root`、`updated_at` 和 `status` 元数据，以及记忆地图、信息优先级、上下文重建、回写和安全规则。

导航路径一律相对于项目根目录，不得使用用户主目录、盘符绝对路径或 `..` 逃逸。导航文件不承载具体任务事实；具体事实存放在对应记忆文件中。

修改目录结构时，先更新导航，再验证所有目标，最后更新平台入口。缺失目标必须显式报告。

