# 上下文重建

## 最小恢复

读取当前任务和当前日志最后一个会话。输出阶段、任务、位置、阻塞和下一步。

## 标准恢复

在最小恢复基础上读取热记忆和相关 ADR。验证任务中引用的代码路径。

## 深度恢复

仅在信息不足或冲突时读取温/冷记忆和定向历史日志。不要一次性加载全部归档。

所有恢复结果应标注待验证信息。当前文件与历史文件冲突时，报告冲突并按照导航中的优先级选择工作基线。

